<Page
    x:Class="UWPExamProject.Pages.WebHttp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPExamProject.Pages"
    xmlns:localHttp="using:UWPExamProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Button Content="HTTP GET" Margin="6" Name="btnTestWebHttp" Click="btnTestWebHttp_Click" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left"/>

        <ListView x:Name="lstWebJsonComments" SelectionChanged="lstWebJsonComments_SelectionChanged" Grid.Row="1" Grid.Column="0" Margin="6">
            <ListView.Header>
                <TextBlock Text="People" FontSize="18" FontWeight="Bold" Margin="4"/>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SWCharacter">
                    <StackPanel Margin="10" Orientation="Vertical">
                        <TextBlock Text="Name:" FontWeight="Bold" Foreground="Gray"/>
                        <TextBlock Text="{x:Bind name}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind gender}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind birth_year}" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="6">
            <TextBlock Text="Starships" FontSize="18" FontWeight="Bold" Margin="4"/>
            <TextBlock Text="{Binding SelectedItem.name, ElementName=lstWebJsonComments}" FontStyle="Italic" Foreground="Gray" Margin="0,0,0,8"/>

            <ProgressRing x:Name="prgStarships" Width="24" Height="24" IsActive="False" Visibility="Collapsed" Margin="0,0,0,8"/>

            <ListView x:Name="lstStarships">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Starship">
                        <Border BorderThickness="0,0,0,1" BorderBrush="#22000000" Padding="6">
                            <StackPanel Orientation="Vertical" Spacing="2">
                                <TextBlock Text="{x:Bind name}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{x:Bind model}" FontStyle="Italic" Foreground="DarkSlateGray"/>
                                <TextBlock Text="{x:Bind manufacturer}" TextWrapping="WrapWholeWords" Foreground="Gray"/>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="Class:" Foreground="DimGray"/>
                                    <TextBlock Text="{x:Bind starship_class}" Foreground="DimGray"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
